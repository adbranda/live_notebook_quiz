const socket=io();const form=document.querySelector('#joinForm'),pinInput=document.querySelector('#pinInput'),nameInput=document.querySelector('#nameInput'),error=document.querySelector('#error'),card=document.querySelector('#joinCard'),game=document.querySelector('#playerGame');const params=new URLSearchParams(location.search);if(params.get('pin'))pinInput.value=params.get('pin');let current=null;const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function err(m){error.textContent=m||''}form.addEventListener('submit',e=>{e.preventDefault();err('');socket.emit('player:join',{pin:pinInput.value.replace(/\D/g,''),name:nameInput.value})});socket.on('error:message',err);socket.on('player:joined',d=>{current=d.player;card.classList.add('hidden');game.classList.remove('hidden');renderLobby(d.room)});socket.on('room:update',r=>{if(!current)return;if(r.state==='lobby')renderLobby(r);if(r.state==='results'&&r.lastResult)renderPlayerResults(r.lastResult);if(r.state==='finished')renderFinished({leaderboard:[...r.players].sort((a,b)=>b.score-a.score).map((p,i)=>({rank:i+1,name:p.name,score:p.score}))})});socket.on('question:started',renderQuestion);socket.on('answer:accepted',()=>{game.innerHTML=`<div class="card status"><h1>Answer locked in</h1><p>Watch the host screen for the result.</p></div>`});socket.on('question:ended',renderPlayerResults);socket.on('game:finished',renderFinished);socket.on('game:closed',d=>{game.innerHTML=`<div class="card status"><h2>Game closed</h2><p>${esc(d.reason)}</p></div>`});function renderLobby(r){game.innerHTML=`<div class="card status"><div class="eyebrow">JOINED</div><h1>${esc(current.name)}</h1><p>You're in! Wait for the host to start.</p><div class="players">${r.players.map(p=>`<div class="player">${esc(p.name)}</div>`).join('')}</div></div>`}function renderWaiting(r){game.innerHTML=`<div class="card status"><h2>Question ${r.current+1}</h2><p>Get ready…</p></div>`}function renderQuestion(q){current.question=q;game.innerHTML=`<div class="card"><div class="eyebrow">QUESTION ${q.number} / ${q.total}</div><div class="question">${esc(q.question)}</div><div>${Object.entries(q.options).map(([k,v])=>`<button class="answer-button" data-answer="${k}"><b>${k}</b><br>${esc(v)}</button>`).join('')}</div><div class="hint">Hint: ${esc(q.hint||'')}</div><div class="muted" id="countdown"></div></div>`;document.querySelectorAll('.answer-button').forEach(b=>b.onclick=()=>{document.querySelectorAll('.answer-button').forEach(x=>x.disabled=true);b.classList.add('selected');socket.emit('player:answer',{answer:b.dataset.answer})});clock(q.endsAt)}function clock(end){const el=document.querySelector('#countdown');if(!el)return;const tick=()=>{const ms=Math.max(0,end-Date.now());el.textContent=`${(ms/1000).toFixed(1)} seconds`;if(ms>0)requestAnimationFrame(tick)};tick()}function renderPlayerResults(r){const mine=current?.score??0;game.innerHTML=`<div class="card result-card"><div class="eyebrow">CORRECT ANSWER</div><h1>${esc(r.correctAnswer)}</h1><p>${esc(r.rationale)}</p><h2>Leaderboard</h2>${table(r.leaderboard)}</div>`}function table(rows=[]){return `<table class="leaderboard"><tbody>${rows.slice(0,10).map(p=>`<tr><td>${p.rank}. ${esc(p.name)}</td><td>${p.score}</td></tr>`).join('')}</tbody></table>`}function renderFinished(r){game.innerHTML=`<div class="card result-card"><div class="eyebrow">FINAL RESULTS</div><h1>Game complete</h1>${table(r.leaderboard)}</div>`}
